	BRANCHING

1. git branch

2. git branch "milkshake-flavours"

3. git branch

4. git checkout milkshake-flavours
   git branch. 
   It means the milkshake-flavours branch is active.

5. git checkout milkshake-flavours
   nano milkshake.md

6. git status
   It tells me that there is untracked files in the milkshake-flavour branch.

7. git add milkshake.md
   git commit -m "added initial flavours"

8. git log

9. git checkout master

10. git checkout -b history master
    git checkout history
    git log

11. history > history.txt 
    It will overwrite history to history.txt

12. git add "history.txt"
    git commit -m "added history"
    git log

13. git checkout milkshake-flavours 
    git log

14. nano README.md 
    git add "README.md"
    git commit -m "random readme changes"

15. git checkout history 
    cat README.md 

16. rm README.md
    echo "booya" > README.md

17. git add "README.md"
    git commit -m "rewrote readme"

18. git checkout master


    
